import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.movementtracker',
  appName: 'Movement Tracker',
  webDir: 'public'
};

export default config;
